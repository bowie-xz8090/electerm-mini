/**
 * Mini edition identity — keep data / portable dir separate from full electerm.
 */
module.exports = {
  /** Folder under AppData (or portable exe dir) */
  dataDirName: 'electerm-mini',
  /** electron-builder / Windows appId */
  appId: 'org.electerm.electerm-mini',
  productName: 'electerm-mini'
}
